1.change	2008-03-04 
	问题：
		发现同一份元数据库描述，用tdr根据在linux和windows下生成的二进制元数据库文件的hash值不同
	分析：
		比较linux和windows下用tdr生成的二进制元数据库文件，发现ID-Index映射表不同，具体是ID相同的
	iD-Index数据在映射表中的存储位置不同。ID-Index映射表是使用c库的qsort已id为排序关键字进行排序的，因此推断
	linux和windows下qsort实现不同，排序关键字值相同，但排序后其位置值可能不同。通过在linux和windows下使用qsort
	排序的测试程序证实此推断。
	解决方法：
		linux和windows下使用相同实现的qsort算法，因此在tdr_auxtools.c中增加了快速排序算法实现
		
2. add 2008-03-05
	新增需求： 增加一个获取数据成员数组计数信息的接口tdr_get_entry_count
	
3. change 2008-03-05
	问题：
		与mysql建立连接的接口，即使在设置了重连标志，当由于空闲超时断开后，连接重连不上。
	原因：
		网上搜索发现：自Mysql 5.x的某个版本之后，Mysql的自动关闭空闲连接的特性修改了，如果一个连接空闲到超时时间（默认28000秒8小时），再次发起的Reconnect重新连接请求不会被接受，
		需要重新建立新连接，这就导致了SER的重连机制不能正常工作：SER只会在需要操作数据库时去使用同一个连接接口，断开了则发起重新连接请求，而且这个问题短期内SER也不能够解决：）
		处理方法：1.使用Mysql 4.0或4.1版本，如果没有用到Mysql 5的一些新特性比如存储过程触发器之类。
		2.定时重启Mysql服务器或Ser（由于本问题可能同样会影响到其它一些需要Mysql支持的服务器程序，所以重启Mysql服务器为好，但需要检测Mysql服务器不被使用的一个时间重启比较难确定）
		3.设置my.cnf，有mysqld字段内增加参数：[mysqld]port = 3306socket = /tmp/mysql.sockwait_timeout= 500000interactive_timeout = 500000（500000秒约五六天的超时时间，
		可根据实际需要选择一个数据库可能空闲的最长时间稍大的时间值。）重启Mysqld应用即可，也可以在执行mysqld时加-o wait_timeout=500000参数同样效果。在mysql客户端show variable时应该可以看到最后一条从默认的wait_time=28000变成500000了。（可能需要重启机子如果重启Mysqld一直不生效的话：） 
	解决方法：
		在tdr接口中增加一个显示重连dbms的接口，给上层应用一个重连dbms的选择；同时增加一个接口自动重连的配置，如果打开此配置，在执行query之后，检查sql执行错误代码，如果是连接断开标志，则重建新连接
		
4 add 2008-03-10
	新增功能： 增加一个数据内存拷贝的接口tdr_copy
	
		
5 modify 2008-03-24
	将结构打包属性“sizetype”改成“sizeinfo”属性

6 modify 2008-03-25
	成员"io"支持字符串格式的属性值：nolimit,noinput,nooutput,noio	
	
7. add 2008-04-14
	新增功能： 提供根据成员在结构中的路径名获取成员句柄的接口		
	
8. change 2008-06-04
	问题：
		如果定义了向后引用的指针类型，则tdr工具会可能会崩溃。
	原因：
		如果成员类型为指针类型，且其指向的类型不存在，则tdr不会发现此问题，这时成员
		的类型数据会错误	
	解决方法：
		加强类型检查
		
9. change 2008-06-06
	新增功能： 提供宏定义组功能，整数类型的成员可以绑定到特定宏定义组，绑定了宏定义组的
	成员其输入输出优先以宏定义名进行处理
	

11. change 2008-06-24
	新增功能： tdr_dbdriver_open_handle接口支持从选项文件中读取参数
	新增功能： 提供根据成员在结构中的路径名获取成员句柄的接口		
	

12. change 2008-06-30
	创建元数据描述库时结构体的当前版本原来是取子成员版本中的最大值，现修改为取子孙成员
	中版本的最大值
	
13. add 2008-08-13
	支持结构体成员数据成员展开存储

14. add 2008-10-19
	增加遍历数据的接口，其思想是实现遍历数据的框架接口，通过回调函数满足调用方的数据访问需求
	
15. add 2008-11-03
	支持数据成员定义auto increment属性

16. add 2008-11-05
    元数据描述支持自定义扩展属性，增加了customattrxml标记，并增加获取相应属性值的接口

 17. add 2008-11-20
    缺省值支持宏定义
	
	tdr uint 缺省值不支持16进制表示

18 add 2008-12-09
      tdr增加一个查询数据库表总条数select count(*)的接口
      
18 add 2008-12-19
     结构体的元数据描述增加uniqueentryname属性，通常结构体成员展开存储到数据库表中时，要求展开后成员名是唯一的，
     但通过此属性来指示TDR接口即使结构体展开时成员名不唯一也要保证展开存储到数据库表中。
     uniqueentryname属性属性值为“true”或“false”，缺省属性值为“true”。如果属性值为“false”，则结构体子孙成员
     展开存储时成员名可以不唯一
     
     
20 modify 2009-01-14
     生成SQL语句时对于整数类型，不自动添加''
     
     
21 modify 2009-01-14
     生成SQL语句时对于整数类型，不自动添加''
     
22 modify 调整优化tdr_hton tdr_ntoh

22 modify 
	通过xml描述生成元数据描述库时，如果发现结构体、成员和宏的名字串超过了最大字符限制，则直接报错，而不是简单截断处理。
	tdr工具支持通过 根据错误号查询具体错误信息的功能